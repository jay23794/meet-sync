# Install for a specific app
npm install express --workspace=apps/api
npm install socket.io --workspace=apps/signaling
npm install react --workspace=apps/web

# Install a dev tool for the whole repo
npm install typescript --save-dev  # at root